steps:
# This step copies deployment script and runs it
- name: 'gcr.io/cloud-builders/gcloud'
  entrypoint: bash
  args:
  - '-c'
  - |
    set -x && \
    apt-get install jq && \
    chmod +x deployment.sh && \
    export PROJECT_ID && \
    echo $PROJECT_ID && \
    export DEPLOYMENT_ID="label-testing" && \
    ./deployment.sh
